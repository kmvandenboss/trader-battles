# Seed data

Deterministic demo seed authoring (Phase 3): ≥40 traders, ≥5 firms (MFFU,
Tradeify, Apex, Topstep — demo labels only, no real partnership implied),
≥150 battles, ratings, badges, notifications, and the demo user KevinV with
real history.

Rules: seeded randomness only — the same scenario must replay identically.
Regenerate via `npm run seed`.
